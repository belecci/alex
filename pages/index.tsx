import type { NextPage } from "next";
import LoginPage from "./components/LoginPage";

const Home: NextPage = () => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default Home;
